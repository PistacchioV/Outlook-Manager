Flask>=3.0
# pywin32 SÓ é necessário/instalável no Windows (com Outlook desktop).
# Em Windows: pip install pywin32
pywin32>=306 ; sys_platform == "win32"

# OPCIONAL: resumo/resposta via Gemini. Sem credenciais no ambiente, o app
# nem usa — segue 100% local. Veja genai_backend.py para as variáveis.
google-genai>=1.0
